development:
  clients:
    default:
      uri: <%= ENV['MONGO_URL'] %>
  options:
    raise_not_found_error: false

test:
  clients:
    default:
      uri: <%= ENV['MONGO_URL'] %>
  options:
    raise_not_found_error: false

production:
  clients:
    default:
      uri: <%= ENV['MONGO_URL'] %>
  options:
    raise_not_found_error: false
